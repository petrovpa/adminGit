Документ для того, кто будет заниматься этим в мой отпуск.

Пункты по которым шёл бы я:

1) Иницииировать добавление фронт-енд функционала.

   Должны сделать кнопку "Войти через ЕСИА" на главной ЛК,
   которая будет инициировать сценарий описанный в начале класса EsiaAuthentificator.java

2) Фронтендеру необходимо реализовать логику сценария.
3) Бэкэндеру надо протестировать получение информации о пользователе (метод getAuthentificatedUserInfo)

   Для этого необходимо получить authCode (метод getAuthUrl возвращает authCode на страницу редиректа)
   и протестить его либо локально (например, копипастнуть его и вызвать в Main.java с этим кодов к кач-ве параметра),
   либо путем коллаборация с фронтендером.

4) Затем бэкендеру дописать авторизацию пользователя

   В PA2LoginCustomFacade.java, названия методов: dsPA2EsiaWebLoginStart, dsPA2EsiaWebLoginEnd

5) Если все предыдущие пункты сделаны - все заработает.
6) Переносим на тест, показываем заказчику.
7) Меняем конфигурацию на прод данные (получить от СБСЖ), накатываем на прод.
8) Вы восхитительны!